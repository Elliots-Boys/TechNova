const supabaseClient = window.supabase.createClient(window.TECHNOVA_SUPABASE_URL, window.TECHNOVA_SUPABASE_PUBLISHABLE_KEY);
function showAuthMessage(message,good=false){const el=document.querySelector('#authMessage');if(el){el.textContent=message;el.className='auth-message '+(good?'good':'');}}
async function signUp(){const name=document.querySelector('#name')?.value.trim(),email=document.querySelector('#email').value.trim(),password=document.querySelector('#password').value;if(!email||password.length<8){showAuthMessage('Enter a valid email and a password of at least 8 characters.');return;}const {error}=await supabaseClient.auth.signUp({email,password,options:{data:{display_name:name||email.split('@')[0]},emailRedirectTo:location.origin+'/pages/account.html'}});if(error){showAuthMessage(error.message);return;}showAuthMessage('Account created. Check your email to confirm your address, then log in.',true);}
async function signIn(){const email=document.querySelector('#email').value.trim(),password=document.querySelector('#password').value;const {error}=await supabaseClient.auth.signInWithPassword({email,password});if(error){showAuthMessage(error.message);return;}location.href='account.html';}
async function signOut(){await supabaseClient.auth.signOut();location.href='../index.html';}
async function loadAccount(){const {data:{user}}=await supabaseClient.auth.getUser();if(!user){location.href='login.html';return;}const {data:profile}=await supabaseClient.from('profiles').select('display_name,bio').eq('id',user.id).maybeSingle();document.querySelector('#accountEmail').textContent=user.email||'';document.querySelector('#accountName').textContent=profile?.display_name||user.email?.split('@')[0]||'TechNova member';const {data:registrations}=await supabaseClient.from('event_registrations').select('event_id,events(title,event_date,location)').eq('user_id',user.id).order('created_at',{ascending:false});const list=document.querySelector('#myEvents');list.innerHTML=registrations?.length?registrations.map(r=>`<div class="feature"><span class="icon">🎟️</span><div><b>${r.events?.title||'Event'}</b><br><span class="muted">${r.events?.event_date||''} · ${r.events?.location||''}</span></div></div>`).join(''):'<p class="muted">You have not registered for any events yet.</p>';}
async function registerForEvent(eventId){const {data:{user}}=await supabaseClient.auth.getUser();if(!user){location.href='login.html?next=events.html';return;}const {error}=await supabaseClient.from('event_registrations').insert({event_id:eventId,user_id:user.id});if(error&&error.code!=='23505')throw error;alert(error?.code==='23505'?'You are already registered for this event.':'Registration saved to your TechNova account.');}
document.addEventListener('DOMContentLoaded',()=>{document.querySelector('#signupBtn')?.addEventListener('click',signUp);document.querySelector('#loginBtn')?.addEventListener('click',signIn);document.querySelector('#logoutBtn')?.addEventListener('click',signOut);if(document.querySelector('#myEvents'))loadAccount();});
